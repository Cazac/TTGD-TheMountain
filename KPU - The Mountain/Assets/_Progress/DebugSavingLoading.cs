using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class DebugSavingLoading : MonoBehaviour
{

    private void SaveNewCharecter()
    {
        /*
        // Save an int array
        ES3.Save<int[]>("myArray", myArray);

        // Save a List of strings
        ES3.Save<List<string>>("myList", myList);

        // Save a Dictionary of string keys and Vector3 values
        ES3.Save<Dictionary<string, Vector3>>("myDictionary", myDictionary);
    }


    private void LookForCharecterSlot()
    {


    }

    private void LoadOldCharecter()
    {
        // Load the int array
        myArray = ES3.Load<int[]>("myArray");

        // Load the List of strings
        myList = ES3.Load<List<string>>("myList");

        // Load the Dictionary of string keys and Vector3 values
        myDictionary = ES3.Load<Dictionary<string, Vector3>>("myDictionary");


        // Load the int array
        myArray = ES3.Load<int[]>("myArray");

        // Load the List of strings
        myList = ES3.Load<List<string>>("myList");

        // Load the Dictionary of string keys and Vector3 values
        myDictionary = ES3.Load<Dictionary<string, Vector3>>("myDictionary");

    */
    }




    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
